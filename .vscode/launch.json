{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "flutter_application_1 (chrome)",
            "request": "launch",
            "type": "dart",
            "deviceId": "chrome",
            "cwd": "${workspaceFolder}"
        }
    ]
}